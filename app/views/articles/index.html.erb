<h1>Articles</h1>
<ul>
  <% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article_path(article) %> •
    <%= link_to '🗑', article_path(article), method: :delete, data: {confirm: "Delete #{article.title}?"} %>
  </li>
  <% end %>
</ul>
<p><%= link_to '✚Add a new article', new_article_path %></p>
